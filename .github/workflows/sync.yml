name: Sync Fork

on:
  schedule:
    - cron: 0,30 10,11 * * *
  workflow_dispatch:  # on button click
  
jobs:
  sync:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
        with:
          ref: master
      - name: Sync Upstream
        run: |
          git fetch julien-klaus/vorkurs_informatik
          git checkout master
          git merge julien-klaus/vorkurs_informatik/master
          git push
